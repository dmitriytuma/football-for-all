@model ClubViewModel
@{
    var countryListItems = Model.CountriesItems.Select(c => new SelectListItem(c.Value, c.Key));
    var stadiumListItems = Model.StadiumsItems.Select(s => new SelectListItem(s.Value, s.Key));
}

<h4>
    Create club
    <small class="text-muted"> | <a asp-action="Index">Back to Clubs</a></small>
</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CountryId" class="control-label"></label>
                <select asp-for="CountryId" class="form-control" asp-items="countryListItems"></select>
                <span asp-validation-for="CountryId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="HomeStadiumId" class="control-label"></label>
                <select asp-for="HomeStadiumId" class="form-control" asp-items="stadiumListItems"></select>
                <span asp-validation-for="HomeStadiumId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="FoundedOn" class="control-label"></label>
                <input asp-for="FoundedOn" class="form-control" />
                <span asp-validation-for="FoundedOn" class="text-danger"></span>
            </div>
            <button class="btn btn-primary" type="submit">Create</button>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
